# Railway config — FastAPI lives at src/api/main.py (not repo root).
# See docs/DEPLOYMENT.md for env vars and health checks.

[deploy]
startCommand = "uvicorn src.api.main:app --host 0.0.0.0 --port $PORT"
