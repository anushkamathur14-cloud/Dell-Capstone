{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["pip install -r requirements-railway.txt"]
    }
  },
  "deploy": {
    "startCommand": "PYTHONPATH=. python -m uvicorn src.api.main:app --host 0.0.0.0 --port ${PORT:-8080}"
  }
}
